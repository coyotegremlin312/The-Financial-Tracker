import React from 'react';
import NavBar from './NavBar';

const Header = () => (
    <header className="Header">
        <h1>The Financial Tracker</h1>
        <NavBar />
    </header>
)


export default Header;